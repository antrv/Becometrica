# Becometrica

The repository contains various useful utilities:

- Becometrica.Unsafe project aggregates all unsafe code and implements types extremely useful for interop with the native code:   
  - Pointers and constant pointers to an unmanaged structure
  - Pointers to a UTF-8 string
  - Conversion utilities
